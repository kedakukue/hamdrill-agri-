# Copy to .env and fill in values (never commit .env)

# Random string — generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=change-me-to-a-long-random-string

# From Formspree: New Form → Integration → form endpoint (https://formspree.io/f/xxxxxxxx)
FORMSPREE_ACTION=https://formspree.io/f/YOUR_FORM_ID

WHATSAPP_NUMBER=263242705863
WHATSAPP_DISPLAY=+263 (242) 705863
CONTACT_EMAIL=administration@hondehgroup.com

# Production hosts (Render, etc.) set FLASK_DEBUG=0 or omit it
FLASK_DEBUG=1
